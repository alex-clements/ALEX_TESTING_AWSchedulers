{
  "name": "auth",
  "version": "1.0.0",
  "description": "authentication and authorization related lambdas",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-jwt-verify": "^4.0.0"
  }
}
